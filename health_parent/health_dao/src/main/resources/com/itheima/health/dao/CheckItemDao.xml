<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace:要关联的接口的全限定名 -->
<mapper namespace="com.itheima.health.dao.CheckItemDao">

    <select id="findAll" resultType="CheckItem">
        select * from t_checkitem
    </select>

    <insert id="add" parameterType="CheckItem">
        insert into t_checkitem (code,name,sex,age,price,type,remark,attention) values
        (#{code},#{name},#{sex},#{age},#{price},#{type},#{remark},#{attention})
    </insert>

    <select id="findPage" parameterType="String" resultType="CheckItem">
        select * from t_checkitem
        <where>
            <if test="value != null and value.length > 0">
                name like #{value} or code like #{value} or remark like #{value}
            </if>
        </where>
    </select>

    <delete id="delete" parameterType="int">
        delete from t_checkitem where id = #{id}
    </delete>

    <select id="countCheckItemById" parameterType="int" resultType="int">
        select count(0) from t_checkgroup_checkitem where checkitem_id = #{id}
    </select>

    <select id="findById" resultType="CheckItem">
        select * from t_checkitem where id = #{id}
    </select>

    <update id="update" parameterType="checkitem">
    update t_checkitem
    set
    code=#{code},
    name=#{name},
    sex=#{sex},
    age=#{age},
    price=#{price},
    type=#{type},
    remark=#{remark},
    attention=#{attention}
    where id=#{id}
    </update>
</mapper>